import styled from 'styled-components';

const Root = styled.div`
  display: flex;
  flex-flow: center;
  justify-content: center;
  height: calc(100% - 74px);
  margin-top: 60px;
`;

export default { Root };
